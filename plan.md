1. **Reply to the comment**: The comment asks for the difference between this PR and #265. I should explain that #265 optimized `ApplyNumFunc` (numerical parsing), while this PR optimizes `ApplyStrFunc` (string parsing). I will reply directly to the comment explaining this.
2. **Submit changes**: I need to use `submit` tool, which completes the code review flow on the PR.
